{
  "name": "mobile-mode-mgk",
  "version": "1.2.1",
  "description": "Mobile-friendly interface for Foundry VTT with touch controls, optimized character sheets, and performance enhancements.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint src/",
    "test": "node test/smoke.mjs",
    "check": "npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "eslint": "^9.0.0"
  }
}
